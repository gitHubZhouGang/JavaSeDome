package com.collection;

import java.util.LinkedList;

import org.junit.Test;

/**
 * 此类主要是为了学习LinkedList
 * 
 * @see LinkedList
 * @see LinkedList.Node
 * @author Cloop
 *
 */
public class LinkedListDome {

	@Test
	public void initLinkedList(){
		LinkedList<String> llist = new LinkedList<>();
		/**
		 * LinkedList的核心点在于其内置类 Node
		 * 	Node 
		 * 	 1,封装了当前对象的值
		 *   2，提供下一个Node
		 *   3，提供了上一个Node
		 * 因为LinkedList 的数据结构问题   所以不建议直接使用for+get(index)方式进行迭代
		 * 
		 */
	}
}
